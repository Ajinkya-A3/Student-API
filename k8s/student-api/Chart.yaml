apiVersion: v2
name: student-api
description: Helm chart for the Student API application (FastAPI-style backend + PostgreSQL), converted from raw Kubernetes manifests
type: application
version: 0.1.0
appVersion: "1.0.0"
