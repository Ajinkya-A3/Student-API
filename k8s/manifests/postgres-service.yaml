---
# Source: student-api/templates/postgres-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: student-api
  labels:
    app: postgres
spec:
  clusterIP: None   # headless - required as the StatefulSet's serviceName
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
