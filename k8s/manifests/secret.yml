apiVersion: v1
kind: Secret
metadata:
  name: student-api-secret
  namespace: student-api
type: Opaque
data:
  # base64 for "postgres" - single key, referenced explicitly as
  # POSTGRES_PASSWORD in the postgres StatefulSet (see env: there)
  DB_PASSWORD: cG9zdGdyZXM=