APP_NAME=Student API

APP_VERSION=1.0.0

APP_ENV=development

DEBUG=false

LOG_LEVEL=INFO

# --- Discrete DB credentials (composed into DATABASE_URL in app/config.py) ---
DB_DRIVER=postgresql+psycopg
DB_HOST=localhost
DB_PORT=5432
DB_NAME=student_db
DB_USER=postgres
DB_PASSWORD=postgres
